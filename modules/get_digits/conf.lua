local conf = {
  action_map = {

    get_digits = {
      mod = "get_digits",
      func = "get_digits",
    },

    flush_digits = {
      mod = "get_digits",
      func = "flush_digits",
    },

  }
}

return conf
