local conf = {
  action_map = {

    format_string = {
      mod = "format",
      func = "format_string",
    },

    format_date = {
      mod = "format",
      func = "format_date",
    },

  }
}

return conf
