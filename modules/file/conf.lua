jester.action_map.create_directory = {
  mod = "file",
  handlers = {
    filesystem = "create_directory",
    default = "create_directory",
  }
}
jester.action_map.remove_directory = {
  mod = "file",
  handlers = {
    filesystem = "remove_directory",
    default = "remove_directory",
  }
}
jester.action_map.move_file = {
  mod = "file",
  handlers = {
    filesystem = "move_file",
    default = "move_file",
  }
}
jester.action_map.delete_file = {
  mod = "file",
  handlers = {
    filesystem = "delete_file",
    default = "delete_file",
  }
}
jester.action_map.file_exists = {
  mod = "file",
  handlers = {
    filesystem = "file_exists",
    default = "file_exists",
  }
}
jester.action_map.file_size = {
  mod = "file",
  handlers = {
    filesystem = "file_size",
    default = "file_size",
  }
}

