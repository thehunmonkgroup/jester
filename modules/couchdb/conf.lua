jester.action_map.couchdb_retrieve_document = {
  mod = "couchdb",
  func = "retrieve_document",
}

jester.action_map.couchdb_create_document = {
  mod = "couchdb",
  func = "create_document",
}

jester.action_map.couchdb_update_document = {
  mod = "couchdb",
  func = "update_document",
}

jester.action_map.couchdb_delete_document = {
  mod = "couchdb",
  func = "delete_document",
}

jester.action_map.couchdb_add_attachment = {
  mod = "couchdb",
  func = "add_attachment",
}

jester.action_map.couchdb_retrieve_attachment = {
  mod = "couchdb",
  func = "retrieve_attachment",
}

jester.action_map.couchdb_delete_attachment = {
  mod = "couchdb",
  func = "delete_attachment",
}

